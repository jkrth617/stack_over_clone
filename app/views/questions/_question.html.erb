
<% @questions.each do |q| %>
  <div class = "question-header">
    <%= q.total_points %>
    <%= link_to q.title, question_path(q) %>
  </div>
  <div class = "question-content">
    <%= q.body %>
  </div>
<% end %>
