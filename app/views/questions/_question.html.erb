<%= button_to "⟰", questions_vote_path, params: {question_id: "1", vote_direction: 1} %>
<%= button_to "⤋", questions_vote_path, params: {question_id: "1", vote_direction: 1} %>

<% @questions.each do |q| %>
  <%= q.title %>
  <%= q.body %>
<% end %>
