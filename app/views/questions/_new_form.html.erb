<%= flash[:errors] %>
<%= form_for @question do |f|%>
  <%= f.text_field :title, placeholder: 'Title' %>
  <%= f.text_area :body, placeholder: 'Body' %>
    <%= fields_for @tag do |ff| %>
      <%= ff.text_field :description, placeholder: 'Tags' %>
    <% end %>
  <%= f.submit %>
<% end %>
